button_skin_action() {
    font-size: 26px;
    font-weight: 300;
    line-height: 36px;

    text-align: center;
    vertical-align: middle;

    background: #fedd61;

    transition: background .3s ease-in;

    border-radius: 6px;

    &:active {
        background: #fd0;
    }
}
