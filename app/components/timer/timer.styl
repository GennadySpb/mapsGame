.timer {
    font-size: 16px
    line-height: 50px;

    position: absolute;
    left: 12px;
    top: 12px;
    text-shadow: 0 1px 0 #fff;

    width: 50px;
    height: 50px;

    text-align: center;
    background: rgba(0,0,0, .2);
    border-radius: 50%;
    box-shadow: 0 0 10px #fff;
    border: 1px solid #999;

    disabled()
}
